using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShowStats : MonoBehaviour
{

    [SerializeField] public object[] chara1Stats = new object[] { "Rosa", 1,2,3,4,5,6,7,8};
    [SerializeField] public object[] chara2Stats = new object[]{"Kachina", 8,7,6,5,4,3,2,1 };
    [SerializeField] public object[] chara3Stats = new object[] {"Lola", 3,5,8,5,0,1,3,7};
    [SerializeField] public object[] chara4Stats = new object[] {"Margot",5,7,0,1,6,8,7,3};

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
